FACADE


We should use this design pattern if we want to hide
complicated logic behind simple interface - facade.

We have here following classes:
- class of facade: this class hides complicated logic
and returns simple methods. For instance: class ToyFacade
which creates toys and add them specified properties.
